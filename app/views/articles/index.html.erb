<h1>Hacker News</h1>
<%= form_tag articles_path, method: :get do %>
<p>Вибрати дати: <%= text_field_tag 'search[date_from]', @search.date_from %>
  <%= text_field_tag 'search[date_to]', @search.date_to %></p>
<%= submit_tag 'Search', class: "btn btn-info" %>
  <% end %>

<table>
  <thead>
  <tr>
    <th>Title</th>
    <th>Date of addition</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @articles.each do |article| %>
    <tr>
      <p><td><%= article.title %></p>
      <p><a href=<%= article.link %>> Читати повністю> </a></p>
      <td><%= article.date %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>